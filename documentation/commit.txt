BUG FIX: Incorrect pages shown when switching modes

This commit:
  - Fixes the bug where pages and page numbers are displayed
    incorrectly when switching between main menu mode and
    all tickets mode