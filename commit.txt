Retrieve tickets in a range (by ID)

This commit:
 - Initializes a skeleton for MODE_PAGINATION
 - Introduces a getTicketsInRange() method to retrieve a ticket with a start and end ID (sequentially)
 - Uses the tabulate library to print the tickets of the range of IDs selected