Error Management: Error message when user tries to move to a page out of bounds

This commit:
  - Identifies whether a next page or previous page exists
  - Prints an error message if user tries to go beyond the first or last page
