Connect to Zendesk API and define ERROR macros

This commit:
 - Connects to the Zendesk API with an API token (not password)
 - Retrieves all tickets on the Zendesk account
 - Defines all ERROR macros to verify successful GET requests