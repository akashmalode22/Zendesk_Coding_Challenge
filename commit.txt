Refactor: User Input Validation based on current mode

This commit:
 - Detects the current mode and does user input validation
 - Every mode has different menu options stored in a list
 - Input validation for selecting a particular ID should be in range of number
   of tickets available