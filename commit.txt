Error Management: Appropriate message displayed for response status codes

This commit:
  - Handles error codes: 401, 404, 500
    - In the case its not 200
  - Recognizes if the response status code is not 200
  - Displays the appropriate error message
