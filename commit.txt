Unittest: New tests for modes.py

This commit:
  - Adds new tests for modes.py functions
  - Ignores some functions for unittest coverage because its constituent
    functions are already unit tested (# pragma: no cover)