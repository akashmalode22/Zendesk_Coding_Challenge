Refactor: Asking for a selected ticket from pagination menu

This commit:
 - Refactors retrievetickets.py to reuse previously implemented function
 - If the user is in pagination mode and asks for a selected ticket,
   the program calls handleSelectedTicketMode() that was previously designed