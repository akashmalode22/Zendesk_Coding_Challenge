Define main loop with mode selection

This commit:
 - Defines the main running loop of the ticket viewer
 - Defines a printer class to print variety of messages
 - Defines a mode class that validates the current mode selected, and changes the mode