Display user feedback if GET request response takes long

This commit:
  - Displays user feedback message to wait for a response in case
    the GET request is taking a while
  - Cleans up some print statements
