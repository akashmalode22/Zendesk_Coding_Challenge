Receive total number of tickets

This commit:
 - Modifies the URL to /api/v2/tickets/count.json
 - Updates the class variable to include the retrieved count
 - This count is used to determine how many tickets to show during "show all tickets"